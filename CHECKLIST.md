# Чеклист проверки перед деплоем на Cloudflare Pages

## ✅ Проверка статичности сайта

- [x] Нет серверных зависимостей (PHP, Python, Ruby, Node.js сервер)
- [x] Все пути к ресурсам относительные (`./assets/...`)
- [x] Нет внешних API вызовов (кроме WhatsApp)
- [x] Форма обрабатывается через JavaScript (не отправляется на сервер)
- [x] Все ресурсы (CSS, JS, изображения) находятся локально

## ✅ Проверка навигации

- [x] Все кнопки навигации имеют атрибут `data-nav-link`
- [x] Все страницы имеют атрибут `data-page`
- [x] Функция `switchPage` доступна глобально
- [x] Обработчики событий добавляются корректно
- [x] Fallback скрипт для file:// протокола добавлен

## ✅ Проверка конфигурационных файлов

- [x] `_headers` - настроены HTTP заголовки для кеширования
- [x] `_redirects` - настроены редиректы для SPA (fallback на index.html)
- [x] `wrangler.toml` - конфигурация для Wrangler CLI
- [x] `package.json` - конфигурация npm (опционально)
- [x] `.gitignore` - настроен правильно

## ✅ Проверка структуры файлов

- [x] `index.html` в корне проекта
- [x] `assets/css/style.css` существует и загружается
- [x] `assets/js/script.js` существует и загружается
- [x] Все изображения в `assets/images/` на месте
- [x] `marketly-gallery.html` существует (если используется)

## ✅ Проверка функциональности

- [x] Навигация между страницами работает
- [x] Форма заказа обрабатывается через WhatsApp
- [x] Фильтры проектов работают (если есть)
- [x] Все внешние ссылки открываются в новой вкладке
- [x] Иконки (ionicons) загружаются с CDN

## ✅ Проверка для Cloudflare Pages

- [x] Framework preset: `None` (статический сайт)
- [x] Build command: пусто (не требуется сборка)
- [x] Build output directory: `.` (корень проекта)
- [x] Root directory: (пусто, если проект в корне репозитория)

## 📝 Заметки

- Форма отправляется через WhatsApp (не требует сервера)
- Все пути относительные, работают на любом домене
- Сайт полностью статический, готов к деплою
- Fallback скрипт обеспечивает работу даже через file:// протокол

## 🚀 Готово к деплою!

Все проверки пройдены. Сайт готов к деплою на Cloudflare Pages.

